[Unit]
Description=Poll

[Service]
User=poll
WorkingDirectory=/var/www/poll
ExecStart=flask run --host=0.0.0.0 --port=8181
Restart=always
Environment='REDIS_HOST={{ hostvars['redis'].ansible_host }}'
